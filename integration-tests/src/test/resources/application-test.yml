spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  
  flyway:
    enabled: true
    clean-disabled: false

logging:
  level:
    root: WARN
    com.healthassistant: INFO
    org.springframework: WARN
    org.testcontainers: WARN

