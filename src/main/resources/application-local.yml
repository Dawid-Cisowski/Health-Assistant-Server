# Local development profile
spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

logging:
  level:
    root: INFO
    com.healthassistant: DEBUG
    org.springframework.web: INFO
    org.hibernate: WARN

